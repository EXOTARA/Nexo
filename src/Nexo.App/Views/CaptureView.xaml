<UserControl x:Class="Nexo.App.Views.CaptureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Captura" Style="{StaticResource SectionTitleStyle}" />
            <TextBlock Margin="0,4,0,0"
                       Text="Capturar, editar y consultar una imagen sin abandonar tu trabajo."
                       Style="{StaticResource MutedTextStyle}" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,18,0,0" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Margin="0,0,6,6" Height="62" IsEnabled="False"
                    Style="{StaticResource SecondaryButtonStyle}" Content="Región" />
            <Button Grid.Column="1" Margin="6,0,0,6" Height="62" IsEnabled="False"
                    Style="{StaticResource SecondaryButtonStyle}" Content="Ventana" />
            <Button Grid.Row="1" Margin="0,6,6,0" Height="62" IsEnabled="False"
                    Style="{StaticResource SecondaryButtonStyle}" Content="Pantalla" />
            <Button Grid.Row="1" Grid.Column="1" Margin="6,6,0,0" Height="62" IsEnabled="False"
                    Style="{StaticResource SecondaryButtonStyle}" Content="Con retraso" />
        </Grid>
    </Grid>
</UserControl>
